server:
  port: 8070

spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  application:
    name: disaster-management
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: dev

logging:
  level:
    org:
      springframework:
        security: DEBUG